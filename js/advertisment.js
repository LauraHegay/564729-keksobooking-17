'use strict';
// модуль, который отвечает за создание пина — метки на карте

(function () {


  var createPin = function (data, index) {
    // var picture = createTemplate(pictureTemplate);
    // picture.dataset.index = index;
    // picture.querySelector('.picture__img').src = data.url;
    // picture.querySelector('.picture__comments').textContent = data.comments;
    // picture.querySelector('.picture__likes').textContent = data.likes;
    // picture.addEventListener('click', onPictureClick);
    return pin;
  };
})();
